import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  listContainer: {
    marginTop: 0
  },
  itemContainer: {
    flexDirection: 'row',
    paddingLeft: 10,
    paddingTop: 5,
  }
});